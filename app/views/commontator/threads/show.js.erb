<%= render :partial => 'show',
           :locals => { :thread => @thread,
                        :user => @user,
                        :page => @commontator_page,
                        :per_page => @commontator_per_page,
                        :modal => @modal_class,
                        :show_all => @show_all } %>

<%= javascript_proc %>
