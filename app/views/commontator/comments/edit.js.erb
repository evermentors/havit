$("<%= @modal_class %> #comment_<%= @comment.id.to_s %>_body_div > .comment-form").html("<%= escape_javascript(
  render :partial => 'form',
         :locals => { :comment => @comment, :modal_class => @modal_class }) %>");
$("<%= @modal_class %> #comment_<%= @comment.id.to_s %>_body_div > .comment-main-body").hide();
var textarea = $("<%= @modal_class %> #comment_<%= @comment.id.to_s %>_body_div > .comment-form .comment_form_field > textarea");
var orig_val = textarea.val();
textarea.focus().val('').val(orig_val);
$("<%= @modal_class %> #comment_<%= @comment.id.to_s %>_div").addClass('editing')

<%= javascript_proc %>
