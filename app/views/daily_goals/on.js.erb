$('.status-goal > .goal-description').text("<%= @description %>".replace(/ nl /g, '\n'))
$('.status-goal > .goal-date > .edit-goal').attr('href', "<%= edit_daily_goal_path(@today_goal)%>")
$('.next-daily-goal-input').val("<%= @tomorrow_goal %>")
