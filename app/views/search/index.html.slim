.search-result-container
  .edit-goal.container-sidebar
    = render 'statuses/edit_goal'
  .search-results.container-center
    .query-result = "'#{@query}'의 검색 결과: #{@groups.count}건"
    - @groups.each do |group|
      = render partial: 'search/searched_group', locals: { group: group }
